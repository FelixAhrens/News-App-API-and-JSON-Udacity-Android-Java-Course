<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="image_layout">
        <item name="android:gravity">center_vertical</item>
        <item name="android:layout_marginLeft">@dimen/margin_horizontal_full</item>
        <item name="android:layout_marginTop">@dimen/margin_vertical</item>
        <item name="android:layout_marginBottom">@dimen/margin_vertical</item>
    </style>

    <style name="text_layout">
        <item name="android:layout_marginTop">@dimen/margin_vertical</item>
        <item name="android:layout_marginBottom">@dimen/margin_vertical</item>
        <item name="android:layout_marginRight">@dimen/margin_horizontal_full</item>
        <item name="android:paddingLeft">@dimen/padding_horizontal</item>
    </style>

    <style name="title">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">0.6</item>
        <item name="android:gravity">bottom</item>
        <item name="android:textColor">@color/primary_text</item>
        <item name="android:maxLines">2</item>
        <item name="android:ellipsize">end</item>
        <item name="android:singleLine">false</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:textSize">@dimen/primary_text_size</item>
        <item name="android:layout_marginRight">@dimen/margin_horizontal_half</item>
    </style>

    <style name="sub_text">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">top</item>
        <item name="android:textColor">@color/secondary_text</item>
        <item name="android:textStyle">italic</item>
        <item name="android:maxLines">2</item>
        <item name="android:textSize">@dimen/secondary_text_size</item>
        <item name="android:layout_marginRight">@dimen/margin_horizontal_half</item>
    </style>

</resources>
